<div class="container">
	<h1>All Users</h1>
	<ul>
		<% @users.each do |user| %>
			<li><%= link_to "#{user.first_name} #{user.last_name}", "/users/#{user.id}"%> <%= user.email %> </li>
		<% end%>
	</ul>	
</div>
